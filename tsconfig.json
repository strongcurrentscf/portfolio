{
  "compilerOptions": {
    "jsx": "react-jsx",
    "baseUrl": "./src", // Base directory for module resolution set to 'src'
    "paths": {
      "components/*": ["components/*"], // Alias for components folder
      "lib/*": ["lib/*"], // Alias for lib folder
      "context/*": ["context/*"], // Alias for context folder
      "hooks/*": ["hooks/*"], // Alias for hooks folder
      "email/*": ["email/*"] // Alias for email folder
    }
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
